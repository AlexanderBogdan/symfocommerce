{% trans_default_domain 'ShopBundle' %}
{#<h4>{{ 'categories.menu.header'|trans }}</h4>#}
<li class="">
    {% for category in categories %}
        <a href="{{ path('category', {'slug' : category.slug}) }}" class="btn btn-action btn-default">
            {{ category.name }}
        </a>
    {% endfor %}
</li>
